# Face detection (needs dlib: see README for installing it on Windows)
face-recognition==1.3.0
# face_recognition_models imports pkg_resources, which setuptools 81+ removed
setuptools<81

# Computer vision and image handling
opencv-python==4.8.1.78
Pillow>=10.1.0
# OpenCV 4.8 is built against NumPy 1.x
numpy>=1.26.0,<2.0
